# Deployment Guide

## Web Deployment (Cloudflare Pages)
- Push to `main` branch.
- Automated via Cloudflare GitHub integration.

## API Deployment (Cloudflare Workers)
- `wrangler deploy` via GitHub Actions.

## Mobile Deployment (Android)
- Automated APK generation via GitHub Actions.
- Manual upload to Play Store (future).
